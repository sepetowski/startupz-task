export interface SectionPointItem {
	countNumber: string;
	desc: string;
}
export interface WorkCard {
	title: string;
	desc: string;
	img: {
		alt: string;
		src: string;
	};
	titleColor: string;
	href: string;
}
